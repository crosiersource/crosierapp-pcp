{% extends '@CrosierLibBase/index.html.twig' %}



{% block page_content %}

    <div class="container" style="margin-bottom: 50px">
        <div class="card">
            <div class="card-header">

                <div class="row">
                    <div class="col-8">
                        <h3>Ficha Técnica</h3>
                    </div>
                    <div class="col-4 text-right">

                        <a class="btn btn-primary" href="{{ url(formRoute) }}" role="button">
                            <i class="fas fa-file" aria-hidden="true"></i> Novo</a>

                        <a class="btn btn-secondary" href="{{ url(listRoute) }}" role="button">
                            <i class="fas fa-sync" aria-hidden="true"></i> Listar</a>

                        {% if e.id is defined and e.id %}
                            <a class="btn btn-info" href="{{ url('fichaTecnica_builder', {'id' : e.id}) }}"
                               role="button">
                                <i class="fas fa-th-list"></i> Insumos</a>
                        {% endif %}


                    </div>
                </div>

            </div>

            <div class="card-body">

                {% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}

                {{ form_start(form) }}
                {{ form_widget(form) }}

                <div class="row">
                    <div class="col text-right">
                        <button type="submit" class="btn btn-primary" value="Salvar">
                            <i class="fas fa-save" aria-hidden="true"></i> Salvar
                        </button>
                    </div>
                </div>


                {{ form_end(form) }}

            </div>


        </div>
    </div>



{% endblock %}



{% block down_scripts %}
    {{ encore_entry_script_tags('loteProducaoForm') }}
{% endblock %}